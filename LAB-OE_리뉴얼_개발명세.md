# LAB-OE 포트폴리오 리뉴얼 — 개발 명세서

> 대상: https://um38s.github.io/LAB-OE/ (GitHub Pages)
> 목표: 콘텐츠·목차·섹션 구성은 그대로 유지하면서 미니멀하고 감각적인 디자인으로 리뉴얼
> 레퍼런스: dennissnellenberg.com (리스트 호버 인터랙션, 스크롤 감각)

---

## 0. 전제 조건 (반드시 준수)

- 콘텐츠, 섹션 구성, 목차, 텍스트 내용은 **현재 그대로 유지** — HTML 구조 변경 최소화
- 기존 이미지 경로(`img/...`) 전부 그대로 사용
- 바닐라 JS 유지 — 빌드 도구(Webpack/Vite 등) 도입 금지
- 외부 라이브러리는 **GSAP + ScrollTrigger + Lenis만** CDN으로 로드
- GitHub Pages 배포 구조 유지 (정적 파일만)

---

## 1. 컬러 시스템 — 라이트/다크 듀얼 모드

### 1-1. CSS 변수 정의

모든 컬러는 `:root` CSS 변수로만 사용. 하드코딩 금지.

```css
/* 라이트 모드 (기본값) */
:root {
  --bg:         #F5F4F0;
  --text:       #141414;
  --text-muted: #787670;
  --line:       #D6D4CE;
  --accent:     #0026FF;
  --preview-shadow: 0 20px 60px rgba(0, 0, 0, 0.15);
}

/* 다크 모드 */
[data-theme="dark"] {
  --bg:         #141414;
  --text:       #F0EFEB;
  --text-muted: #9C9A94;
  --line:       #343434;
  --accent:     #3D5AFE;   /* 다크 배경에서 채도 유지를 위해 한 단계 밝은 블루 */
  --preview-shadow: 0 20px 60px rgba(0, 0, 0, 0.6);
}
```

### 1-2. 포인트 컬러(--accent) 사용처 — 아래 5곳으로 엄격히 제한

1. 리스트 호버 프리뷰의 "View" 원형 버튼 배경
2. 링크/네비게이션 호버 상태
3. `::selection` 배경 (텍스트 선택 색)
4. 커스텀 커서 (작은 원형 도트)
5. 모드 토글 버튼의 활성 상태

그 외 어디에도 블루 사용 금지. 포인트 컬러의 절제가 이 디자인의 핵심.

### 1-3. 모드 토글

- 위치: 우측 상단 네비게이션 영역, 텍스트 메뉴 옆
- 형태: 최소한의 원형 아이콘 버튼 (해/달 아이콘 또는 반원 아이콘, 32px)
- 동작:
  - 클릭 시 `<html>`의 `data-theme` 속성 토글
  - 전환 애니메이션: 배경·텍스트 컬러에 `transition: background-color 0.5s ease, color 0.5s ease` — 페이지 전체가 부드럽게 반전
  - 선택값은 `localStorage`에 저장, 재방문 시 복원
  - 최초 방문 시 `prefers-color-scheme` 감지해서 초기값 결정 (기본 fallback은 라이트)
- **다크 모드 전용 보정 (중요 — 목업 검증에서 발견된 문제들):**
  - 호버 시 비활성 행 디밍: 라이트에서 `opacity 0.3` → 다크에서는 `opacity 0.45` (다크 배경에서 0.3은 거의 안 보임)
  - 호버 프리뷰 이미지: 다크에서는 `border: 1px solid var(--line)` 추가 (야간 촬영 이미지의 어두운 부분이 배경과 녹는 것 방지)
  - 이미지 썸네일 전반에 다크 모드에서만 얇은 보더 적용

---

## 2. 히어로 (랜딩) — 키네틱 타이포그래피

- 배경: `var(--bg)` 그대로, 이미지/영상 없이 타이포만
- "LAB OE" 대형 타이포: 뷰포트 폭의 80~90%, **배리어블 폰트** 사용
  - 추천: Inter Variable 또는 Anybody Variable (Google Fonts, wght + wdth 축 지원 폰트)

### 2-1. 로드 시퀀스

1. "LAB OE": 아래→위 마스크 리빌 (clip-path 방식, GSAP, 0.9s, `power4.out`, 글자별 0.08s 스태거)
2. 서브텍스트 "media artist & exhibition designer": 0.3s 딜레이 후 페이드업
3. SCROLL 인디케이터: 페이드인 후 미세한 상하 루프 모션 (yoyo, 1.5s 주기)

### 2-2. 커서 인터랙션 (데스크톱 전용)

- 커서와 각 글자 사이 거리 계산 → 가까운 글자일수록 보간:
  - `font-weight`: 400 → 800
  - `font-stretch`: 100% → 125% (폰트가 wdth 축 지원 시)
- 영향 반경: 200px / 복원: lerp 0.12로 부드럽게
- 선택 옵션: 커서가 가장 가까운 글자 하나에만 `var(--accent)` 컬러 적용

### 2-3. 유휴(idle) 시 자동 모션

- 마지막 마우스 이동 후 **3초** 경과 시 idle 모드 진입
- 웨이트 웨이브가 좌→우로 흐르는 루프:
  - 글자별 사인파 위상차, 주기 4s
  - weight 범위 **400~650으로 절제** (숨 쉬는 정도, 과한 모션 금지)
- 마우스 움직이면 즉시 idle 해제 → 커서 인터랙션 모드로 전환
- 모바일: 커서 인터랙션 없이 idle 웨이브만 상시 재생

---

## 3. 리스트 호버 인터랙션 (가장 중요한 수정)

대상: `MEDIA ART EXHIBITIONS`, `MUSEUM & EXHIBITION SPACE DESIGN` 섹션의 각 리스트 항목

### 3-1. 호버 시 동작 (데스크톱)

1. **커서 추적 이미지 프리뷰**
   - 해당 항목에 연결된 기존 이미지 사용
   - 크기: 약 340×226px, `object-fit: cover`
   - 커서를 lerp 0.08~0.1로 지연 추적 (즉시 따라오지 않고 살짝 끌려오는 감각이 핵심)
   - `box-shadow: var(--preview-shadow)` / 다크 모드 시 1px 보더 추가 (1-3 참조)
2. **"View" 원형 버튼**
   - 프리뷰 중앙, 지름 88px, 배경 `var(--accent)`, 흰색 텍스트
   - 등장: scale 0→1 (0.3s, `back.out`)
3. **행 상태 변화**
   - 호버된 행: 텍스트 8px 우측 이동 (0.3s ease)
   - 나머지 행: opacity 라이트 0.3 / 다크 0.45 (CSS 변수로 관리)

### 3-2. 예외 처리

- 이미지가 없는 항목(2019~2021 일부 텍스트 전용 항목): 프리뷰 없이 행 상태 변화만 적용
- 모바일: 호버 프리뷰 전체 비활성, 탭 시 바로 해당 링크 이동

---

## 4. 스크롤

- **Lenis** 스무스 스크롤 적용, `lerp: 0.1`
- 섹션 진입 애니메이션 (GSAP ScrollTrigger):
  - 섹션 타이틀: 페이드업 + 20px 상승
  - 리스트 행: 행별 0.05s 스태거로 순차 페이드업
  - trigger: 각 요소가 뷰포트 85% 지점 진입 시, 1회만 재생
- 과한 패럴랙스 금지 — 미니멀 유지

---

## 5. 커스텀 커서 (데스크톱 전용)

- 기본: `var(--accent)` 색 원형 도트 (지름 10px), 실제 커서를 lerp 0.15로 추적
- 링크/버튼 호버 시: 지름 40px로 확대 + 투명도 낮춤 (mix-blend-mode: difference 검토)
- 리스트 항목 호버 시: 커서 숨김 (프리뷰의 View 버튼이 커서 역할 대체)
- 모바일/터치 기기: 완전 비활성, 기본 커서 유지

---

## 6. 타이포그래피 / 여백

- 본문·리스트: 산세리프 (Inter 권장), 한글은 Pretendard Variable 병용
  ```css
  font-family: "Inter", "Pretendard Variable", sans-serif;
  ```
- 리스트 행 높이: 90~100px, 행 사이 1px `var(--line)` 구분선
- 섹션 사이 수직 여백: 160px 이상 (데스크톱), 80px (모바일)
- 좌우 마진: 데스크톱 최소 70px, 콘텐츠 최대 폭 1400px 중앙 정렬

---

## 7. 반응형 / 성능

- 브레이크포인트: 768px (모바일), 1024px (태블릿)
- 모바일에서 비활성: 커서 인터랙션, 호버 프리뷰, 커스텀 커서
- 모바일에서 유지: 히어로 idle 웨이브, 스크롤 페이드업, 모드 토글
- `prefers-reduced-motion: reduce` 시 모든 애니메이션 비활성 (접근성)
- 이미지 lazy loading (`loading="lazy"`) 적용
- Lighthouse 성능 90점 이상 목표

---

## 8. 작업 순서 제안

1. CSS 변수 시스템 + 듀얼 모드 토글 (구조 먼저)
2. 타이포/여백 리셋 (섹션 6)
3. Lenis + 스크롤 애니메이션 (섹션 4)
4. 리스트 호버 프리뷰 (섹션 3) ← 가장 공수 큼
5. 히어로 키네틱 타이포 (섹션 2)
6. 커스텀 커서 (섹션 5)
7. 반응형/접근성/성능 마무리 (섹션 7)

각 단계 완료 시 브라우저에서 확인 후 다음 단계 진행.

---

## 9. 튜닝 파라미터 목록 (구현 후 조정 예정)

구현 시 아래 값들은 상수로 분리해둘 것 (한 곳에서 수정 가능하게):

| 파라미터 | 초기값 | 설명 |
|---|---|---|
| `PREVIEW_LERP` | 0.09 | 호버 프리뷰 커서 추적 지연 |
| `HERO_RADIUS` | 200px | 히어로 커서 영향 반경 |
| `IDLE_DELAY` | 3s | idle 모드 진입 대기 시간 |
| `IDLE_PERIOD` | 4s | idle 웨이브 주기 |
| `IDLE_WEIGHT` | 400–650 | idle 웨이브 웨이트 범위 |
| `DIM_LIGHT` | 0.3 | 라이트 모드 비활성 행 투명도 |
| `DIM_DARK` | 0.45 | 다크 모드 비활성 행 투명도 |
| `SCROLL_LERP` | 0.1 | Lenis 스크롤 감쇠 |
