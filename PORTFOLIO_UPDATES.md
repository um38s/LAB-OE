# Portfolio Update Log — 2026-03-25

이 파일은 포트폴리오 사이트(`portfolio/`)에서 진행된 주요 업데이트 이력과 기술 설계를 정리한 문서입니다.

---

## 주요 변경 사항

### 1. 오타 수정
- `index.html` 및 `resume_text.txt` 내 "초정작가" → **"초청작가"** 수정

---

### 2. 다크 / 라이트 모드 구현

#### 토글 버튼 (`index.html` + `style.css`)
- 우측 상단에 알약(pill) 형태의 고급스러운 테마 토글 스위치 추가
- CSS 변수(`--bg-color`, `--text-primary`, `--point-color` 등) 기반으로 전체 색상 시스템 구성
- `localStorage`로 사용자 선택 저장, `prefers-color-scheme` 시스템 설정 우선 감지

#### 라이트 모드 디자인 원칙
- 배경: `#f4f4f4` (눈이 편안한 웜그레이)
- 텍스트: `#222222` (진한 무채색)
- 포인트 컬러: `#333333` (섹션 제목 강조, 무채색)
- `mix-blend-mode: difference` → `normal` 전환으로 텍스트 가독성 확보

---

### 3. WebGL 배경 파티클 (`three-bg.js`)

#### 테마 연동
- `themeChanged` 커스텀 이벤트로 테마 전환 시 즉각 반영
- **다크 모드**: 오렌지 파티클 (`0x994000`), 기본 크기 (`sizeMultiplier: 1.0`)
- **라이트 모드**: 짙은 회색 파티클 (`0x111111`), 최소 크기 (`sizeMultiplier: 0.7`)

#### 성능 최적화
- 파티클 수: ~~3,000개~~ → **1,200개** (과부하 방지)
- FPS 상한 제거 (30fps 제한 시 마우스 반응이 버벅였음) → 네이티브 `requestAnimationFrame` 사용
- `visibilitychange` 이벤트로 탭 비활성화 시 렌더링 자동 중단
- `const clock = new THREE.Clock()` 선언 위치 주의: **반드시 `animate()` 함수 선언 이전에 위치**

```js
// ✅ 올바른 순서
const clock = new THREE.Clock();
function animate() { ... }
animate();

// ❌ 잘못된 순서 (clock undefined 에러 발생)
function animate() { const elapsedTime = clock.getElapsedTime(); } // clock 미선언
const clock = new THREE.Clock();
```

---

### 4. 모달(Modal) 이미지 뷰어 개선 (`style.css` + `main.js`)

| 항목 | 다크 모드 | 라이트 모드 |
|---|---|---|
| 배경 파티클 색상 | 회색 (`160, 160, 160`) | 회색 (`100, 100, 100`) |
| 유튜브 링크 텍스트 | `var(--text-primary)` | `var(--text-primary)` |
| 인디케이터 점(dot) | `rgba(255,255,255,0.3)` | `rgba(0,0,0,0.2)` |
| Back 버튼 위치 | `top: 6.5rem` (토글 아래) | 동일 |
| Back 버튼 크기 | `72px × 32px` | 동일 (테마 스위치와 동일) |

---

### 5. 모바일 반응형 최적화 (`style.css`)
- Hero 섹션 타이틀: `clamp(3rem, 8vw, 10rem)` 으로 모든 화면에 맞게 유동 조절
- 모달 이미지: 모바일 환경 크기 및 인디케이터 위치 최적화

---

### 6. 원형 다이어그램 (Design Philosophy 섹션)
- **라이트 모드**: 테두리 색상 `rgba(0,0,0,0.15)` → `rgba(0,0,0,0.30)` (더 잘 보이게)
- 라이트 모드에서 내부 텍스트(공간, 다양성 등) `color: var(--text-primary)` 적용
- 가운데 X자 `.cross` 도 라이트 모드에서 진한 회색으로 전환

---

### 7. 타이포그래피
- `KIM YOUNG TAE` 줄: `transform: translateY(-2vh)` 로 위쪽 `LAB OE` 와 시각적으로 밀착되도록 조정
- `MEDIA ARTIST & EXHIBITION DESIGNER` 부제: `margin-left: 0.3em` 으로 좌측 정렬 맞춤

---

## 파일별 주요 수정 목록

| 파일 | 주요 변경 내용 |
|---|---|
| `index.html` | 테마 토글 마크업 추가, 초청작가 오타 수정 |
| `css/style.css` | CSS 변수 다크/라이트 정의, 토글 스타일, 모달/다이어그램 라이트 모드 재정의, 반응형 미디어 쿼리 |
| `js/main.js` | 테마 유지 로직(`localStorage`), 모달 파티클 색상 테마 분기 |
| `js/three-bg.js` | 테마 연동 함수 `applyThreeTheme()`, 성능 최적화, 파티클 파라미터 |

---

## 향후 포인트 컬러 추가 방법

라이트 모드에서 특정 부분에 포인트 컬러를 넣고 싶을 때:

```css
[data-theme="light"] {
    --point-color: #FF6B35; /* 원하는 포인트 컬러로 변경 */
}
```

특정 요소만 변경하려면 해당 선택자에 직접 색상 지정:
```css
[data-theme="light"] .hero-subtitle {
    color: #FF6B35;
}
```

---

### 9. 테마 초기화 및 UI 정밀 조정 — 2026-04-14

#### 테마 초기화 로직 개선 (`index.html`)
- 배경 파티클 색상이 라이트 모드에서 순간적으로 오렌지색으로 보이는 '번쩍임(FOUC)' 현상을 해결하기 위해, `<head>` 최상단에 테마 감지 인라인 스크립트를 추가했습니다.
- 스타일 시트 로드 전에 `data-theme` 속성을 먼저 설정하여 시각적 일관성을 확보했습니다.

#### 디자인 철학(Design Philosophy) 모바일 레이아웃 & 시각적 최적화
- 모바일 환경에서 버블들의 수직 정렬이 어긋나는 문제를 해결하기 위해 도입한 투명 스페이서를 구조화했습니다.
- **첫 번째 그룹**: 'X' 표시를 유지하여 디자인 정체성을 살림.
- **두 번째 그룹**: 'X' 표시를 투명하게 처리(`invisible` 클래스)하여 레이아웃 정렬만 정교하게 유지하도록 개선했습니다.

#### 전시 데이터 및 이미지 업데이트
- **국립경주박물관 (2020)**: 중복 이미지를 제거하고 `s8_01.jpg`를 추가하여 총 4장의 고화질 이미지 리스트를 구성했습니다.
- **국립나주박물관 (2023)**: 요청에 따라 불필요한 이미지를 제외하고 최적의 3장으로 리스트를 조정했습니다.
- **이미지 교체**: 국립경주박물관 불교사원실 이미지를 `s7_002.jpg`로 교체하여 최신 상태를 반영했습니다.

#### 모달(Modal) UI 정밀 위치 조정 (`style.css`)
- **인디케이터(점)**: 가로 방향의 정중앙에서 오른쪽으로 아주 미세하게 `3vw`만큼 이동시켜 이미지와의 시각적 균형을 맞췄습니다.
- **유튜브 버튼**: 가로 방향 오른쪽으로 `4px`, 세로 방향 위쪽으로 `4px` 이동시켜 정밀한 레이아웃을 구현했습니다.

#### 성능 및 코드 최적화
- **성능 전수 최적화**: 사이트 내 모든 이미지에 `loading="lazy"` 속성을 적용하여 초기 로딩 속도를 향상시켰습니다.
- **인라인 스타일 제거**: HTML에 남아있던 모든 `style="..."` 속성을 `style.css`로 이관하여 구조와 스타일을 완전히 분리하고 유지보수성을 높였습니다.
- **데이터 무결성**: 자바스크립트의 이미지 경로 분리 로직을 강화하여 오타나 빈 콤마가 있어도 오류 없이 작동하도록 개선했습니다.

---

### 10. 이미지 모달(Modal) 내비게이션 화살표 추가 — 2026-05-01

#### UI/UX 개선 (`index.html`, `style.css`)
- **버튼 추가**: 모달 내부의 좌우측 중앙에 화살표 버튼(`<i class="fas fa-chevron-left/right"></i>`)을 추가하여 다수 이미지 열람 시 직관적인 이동이 가능하도록 개선했습니다.
- **반응형 디자인**: PC에서는 넉넉한 3rem 크기, 모바일에서는 2rem 크기로 자동 조절되며, 터치 영역(padding)을 여유롭게 주어 모바일 사용성을 높였습니다. 
- **색상 및 테마 연동**: 화살표는 반투명한 색상으로 이미지 감상을 방해하지 않으며, 호버 시 밝아지는 효과를 적용했습니다. 라이트/다크 모드별 가시성도 최적화했습니다.

#### 스크립트 고도화 (`js/main.js`)
- **전역 상태 관리**: 현재 이미지 목록(`currentImageArray`)과 인덱스(`currentImageIndex`) 변수를 스코프 상단으로 분리하여 이전/다음 버튼에서 이미지를 직접 제어할 수 있도록 구조를 개선했습니다.
- **예외 처리**: 이미지가 1장일 경우 화살표 버튼을 자동으로 숨김(`display: none`) 처리하여 불필요한 UI 노출을 방지했습니다.
- **순환(Loop) 구현**: 이전/다음 버튼 클릭 시 맨 앞과 맨 뒤를 자연스럽게 이어주는 순환 로직을 적용했습니다.
- **안정성 (이벤트 전파 방지)**: 버튼 터치 시 모달이 의도치 않게 꺼지는 버그를 막기 위해 `e.stopPropagation()` 및 `e.target.closest('.modal-nav')` 예외 처리 로직을 적용했습니다.
